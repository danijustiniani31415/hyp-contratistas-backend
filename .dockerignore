# Artefactos de compilación (no deben entrar al build del contenedor)
**/bin/
**/obj/

# Control de versiones e IDE
.git/
.github/
.vs/
.vscode/
**/*.user

# Config con SECRETOS: se montan en runtime con -v, NUNCA se hornean en la imagen
appsettings.Production.json
appsettings.Local.json
appsettings.Demo.json
appsettings.Development.json

# Docker y docs (no se necesitan dentro de la imagen)
Dockerfile
.dockerignore
README.md
